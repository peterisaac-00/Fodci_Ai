### Instruction
What should a password reset implementation prove in tests?

### Input
Reset token is submitted to create a new password.

### Response
Prove that tokens are random, single-use, short-lived, protected in storage, invalidated after use or password change, rate-limited, and never returned in logs or public error details.
