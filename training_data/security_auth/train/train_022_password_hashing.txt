### Instruction
How should password reset tokens be designed?

### Input
None

### Response
Generate high-entropy random, single-use, short-lived tokens, store only a protected representation when possible, invalidate them after use or account changes, and never log or put them in referrer-prone URLs unnecessarily.
